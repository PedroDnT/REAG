# Core runtime dependencies.
#
# Everything the analyzers, collectors and report generators import. Keep this
# file minimal: it is what CI installs and what a user needs to run an
# investigation. See requirements-dev.txt (tests, lint), requirements-optional.txt
# (external data providers) and requirements-notebooks.txt (Jupyter).

# Data processing
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0

# Charting for Benford plots and per-fund brief charts
# (src/analyzers/benford_law.py, src/explain/charts.py)
matplotlib>=3.7.0

# Utilities
python-dateutil>=2.8.0
tqdm>=4.65.0

# Pandas optional dependencies. Neither is imported anywhere by name -- both are
# reached through a pandas API that loads them lazily -- so grepping for imports
# does not find them. Removing either produces an ImportError only at the moment
# a report is written.
#
#   tabulate  <- DataFrame.to_markdown()  (src/utils/reporting.py)
#   openpyxl  <- DataFrame.to_excel()     (src/utils/reporting.py, analyzers/base.py)
tabulate>=0.9.0
openpyxl>=3.1.0
